# df_who

This project looks at predicting the life expectancy across countries.

For more information find complete documentation in ./docs/

## Requirements

To install all requirements and add modules to project top, run the following command in terminal
while at this project location:

`pip install -e .`
