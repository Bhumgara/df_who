[project]
name = "who-life-expectancy"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas",
    "statsmodels",
    "scikit-learn",
    "sklearn",
    "seaborn",
    "streamlit",
    "matplotlib",
    "kagglehub",
]

[tool.setuptools]
packages = ["utils"]

[tool.black]
line-length = 100
